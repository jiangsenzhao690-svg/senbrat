# Project Custom Instructions

## Cursor Constraint (CRITICAL)
- **DO NOT TOUCH OR MODIFY THE STAR CURSOR**: Under no circumstances should the custom star cursor (`CustomCursor.tsx`, its usage in `App.tsx`, or cursor styles in `index.css`) be modified, replaced, or removed.
- Always retain the custom rotating glowing green star cursor (`<CustomCursor />`).
